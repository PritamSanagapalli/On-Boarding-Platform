package com.onboarding.model.enums;

/**
 * Status values for onboarding documents.
 *
 * Lifecycle: PENDING → SUBMITTED → APPROVED / REJECTED
 */
public enum DocumentStatus {
    PENDING,
    SUBMITTED,
    APPROVED,
    REJECTED
}
